{
  "name": "bus-live-aolabs",
  "version": "0.1.0",
  "private": true,
  "description": "Minimal WRTA live tracker for routes 2, 4, 3, and 31.",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "check": "node --check server.js && node --check public/app.js"
  },
  "engines": {
    "node": ">=20"
  }
}
